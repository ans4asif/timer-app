language: node_js

node_js:
 -"stable"

 cache:
 dependencies:
 -"node_modules"

 script:
 -npm install
 -npm test
 -npm run build
 
 deploy:
 provider: netlify
 skip_cleanup: true
 domain: "https://timerr-app.netlify.app/"

project: ./build/

on:
branch: master

